services:
  nginx:
    image: nginx:latest
    container_name: trace-log-nginx
    environment:
      TZ: Asia/Seoul
    ports:
      - "8081:80"
    volumes:
      - ./nginx/conf/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/logs:/var/log/nginx
    restart: unless-stopped
